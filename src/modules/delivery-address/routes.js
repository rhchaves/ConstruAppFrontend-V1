import DeliveryAddressPage from 'src/modules/delivery-address/routes';

const routes = [
  {
    path: 'delivery-address',
    name: 'delivery-address',
    component: DeliveryAddressPage,
  },
];

export default routes;
